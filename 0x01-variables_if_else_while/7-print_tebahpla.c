#include <stdio.h>
/**
 *main - print thelowercase  alphabet in reverse
 *Return: Always (success)
 *
 */
int main(void)
{
	int ch;

	for (ch = 'z' ; ch >= 'a' ; ch--)

	putchar(ch);
	putchar('\n');
	return (0);
}

