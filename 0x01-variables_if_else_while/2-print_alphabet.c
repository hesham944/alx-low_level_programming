#include <stdio.h>
/**
 *main - print the alphabet in lowercase
 *Return: Always (success)
 *
 */
int main(void)
{
	int ch;
	for (ch = 'a' ; ch <= 'z' ; ch++)
	putchar(ch);
	putchar('\n');

	return (0);



}
